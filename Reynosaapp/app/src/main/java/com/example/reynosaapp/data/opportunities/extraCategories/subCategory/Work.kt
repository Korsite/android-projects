package com.example.reynosaapp.data.opportunities.extraCategories.subCategory

import com.example.reynosaapp.R
import com.example.reynosaapp.data.framework.SubCategoryData

/**
 *  Insert instances to write items
 *  Consider that, the same amount of instances in an internal val MUST BE the same amount of
 *  instances written in items
 *  @see com.example.reynosaapp.data.opportunities.extraCategories.items.Work
 */
object Work {
    internal val subCategoryWork1 = listOf<SubCategoryData>()
    internal val subCategoryWork2 = listOf<SubCategoryData>()
    internal val subCategoryWork3 = listOf<SubCategoryData>()
}